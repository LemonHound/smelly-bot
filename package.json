{
  "name": "smelly-bot",
  "version": "0.1.0",
  "private": true,
  "description": "A silly Slack bot for a friend group channel. Responds to @-mentions; future plans include LLM-driven GitHub automation.",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "start": "node src/index.js",
    "dev": "nodemon --ignore 'firebase-debug.log' --ignore 'firestore-debug.log' --ignore '.firebase/' --ignore '*.log' src/index.js",
    "test": "node --test test/llm.test.js test/slack.test.js test/mcpClient.test.js test/docCache.test.js test/githubTools.test.js"
  },
  "dependencies": {
    "@anthropic-ai/sdk": "^0.89.0",
    "@google-cloud/firestore": "^7.11.0",
    "@modelcontextprotocol/sdk": "^1.29.0",
    "@slack/bolt": "^3.22.0",
    "dotenv": "^16.4.5",
    "pino": "^10.3.1"
  },
  "devDependencies": {
    "nodemon": "^3.1.14",
    "pino-pretty": "^13.1.3"
  }
}
